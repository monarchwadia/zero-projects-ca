{
  "name": "zero-projects-ca",
  "version": "0.1.0",
  "private": true,

  "//": "If the context is ever not ROOT, set it to that context.. for example, '/zero-projects-ca'",
  "homepage": ".",

  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "mv docs/CNAME ./CNAME && rm -rf docs && react-scripts build && mv build docs && mv ./CNAME docs/CNAME",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
